import GameClient from "@/components/GameClient";

export const metadata = {
  title: "100 vs 100K | Plugd",
  description: "Can you guess which builder has more followers? Play the game and find out!",
};

export default function GamePage() {
  return <GameClient />;
}
